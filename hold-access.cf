user = vmail
password = vmaildbpass
hosts = unix:/run/mysqld/mysqld.sock
dbname = vmail
query = select if(hold = true, 'HOLD', 'OK') AS access from hold where destination_username = '%u' and destination_domain = '%d' LIMIT 1;
